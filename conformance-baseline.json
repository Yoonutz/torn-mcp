{
  "company/applications": [
    "wrong type at /applications/*/player/stats/manual_labor — schema expects integer",
    "wrong type at /applications/*/player/stats/intelligence — schema expects integer",
    "wrong type at /applications/*/player/stats/endurance — schema expects integer"
  ],
  "user/equipment": [
    "wrong type at /equipment/*/stats — schema expects object"
  ],
  "user/stocks": [
    "wrong type at /stocks/*/bonus/increment — schema expects integer",
    "wrong type at /stocks/*/bonus/progress — schema expects integer",
    "wrong type at /stocks/*/bonus/frequency — schema expects integer"
  ]
}
