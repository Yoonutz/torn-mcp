{
  "name": "torn-mcp",
  "version": "0.4.0",
  "description": "Remote MCP server for the Torn City API v2, on Cloudflare Workers",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "generate": "node scripts/generate-endpoints.mjs",
    "sync-openapi": "node scripts/sync-openapi.mjs",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.5.4",
    "vitest": "^3.2.6",
    "wrangler": "^4.14.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
